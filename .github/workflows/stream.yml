name: YouTube Live Stream

on:
  workflow_dispatch:
  schedule:
    # Tento cron výraz spustí stream každý deň o 19:00 UTC
    # Formát: 'minúta hodina deň mesiac deň_v_týždni'
    - cron: '0 19 * * *'

jobs:
  stream:
    runs-on: ubuntu-latest

    steps:
      - name: Inštalácia FFmpeg
        run: |
          sudo apt update
          sudo apt install -y ffmpeg curl

      - name: Stiahnutie videa z Google Drive
        run: |
          # ID súboru z vášho odkazu
          FILE_ID="1ZcsuqfFT-HcRg0luL8Q_4oHh_L1EhYEn"
          echo "Sťahujem veľké video z Google Drive..."
          
          # Použitie exportného odkazu pre priame stiahnutie
          curl -L "https://docs.google.com/uc?export=download&id=${FILE_ID}" -o video.mp4
          
          if [ ! -s video.mp4 ]; then
            echo "CHYBA: Súbor video.mp4 sa nepodarilo stiahnuť alebo je prázdny!"
            exit 1
          fi

      - name: Streamovanie na YouTube
        env:
          YOUTUBE_KEY: ${{ secrets.YOUTUBE_STREAM_KEY }}
        run: |
          if [ -z "$YOUTUBE_KEY" ]; then
            echo "CHYBA: YOUTUBE_STREAM_KEY chýba v GitHub Secrets!"
            exit 1
          fi

          echo "Spúšťam stream na YouTube (Video z Drive + Generované ticho)..."
          
          # Nastavenia optimalizované pre YouTube (podľa OBS štandardu)
          ffmpeg -re -stream_loop -1 -i video.mp4 \
          -f lavfi -i anullsrc=channel_layout=stereo:sample_rate=44100 \
          -map 0:v:0 -map 1:a:0 \
          -c:v libx264 -preset veryfast -b:v 3000k -maxrate 3000k -bufsize 6000k \
          -profile:v main -level 4.1 -bf 0 -pix_fmt yuv420p \
          -r 30 -g 60 -keyint_min 60 -sc_threshold 0 \
          -c:a aac -b:a 128k -ar 44100 -ac 2 -shortest \
          -f flv "rtmp://a.rtmp.youtube.com/live2/$YOUTUBE_KEY"
