name: YouTube Live Stream

on:
  workflow_dispatch:

jobs:
  stream:
    runs-on: ubuntu-latest

    steps:
      - name: Inštalácia FFmpeg a Megatools
        run: |
          sudo apt update
          sudo apt install -y ffmpeg megatools

      - name: Stiahnutie videa z Mega.nz
        run: |
          # Odkaz v úvodzovkách, aby shell neinterpretoval '#' ako komentár
          MEGA_LINK="https://mega.nz/file/4lF02CIQ#LngN-QNggUSnI0mgBXv1-_TIp-UtoXQF3oztRq6IbcU"
          
          echo "Sťahujem video z Mega.nz..."
          
          # megadl niekedy premenuje súbor podľa názvu na Mega. My ho chceme ako video.mp4
          # Skúsime najprv zistiť názov súboru alebo ho vynútiť
          megadl "$MEGA_LINK" --path .
          
          # Nájdeme stiahnutý súbor (keďže megadl ignoruje --path pre názov súboru niekedy) 
          # a premenujeme ho na video.mp4
          STIAHNUTY_SUBOR=$(ls | grep -v "stream.yml" | grep -E ".mp4|.mkv|.avi|.mov" | head -n 1)
          
          if [ -z "$STIAHNUTY_SUBOR" ]; then
            echo "CHYBA: Súbor sa nepodarilo stiahnuť z Mega.nz."
            ls -R
            exit 1
          fi
          
          mv "$STIAHNUTY_SUBOR" video.mp4
          
          # Kontrola integrity
          FILE_TYPE=$(file video.mp4)
          echo "Detegovaný typ: $FILE_TYPE"
          ls -lh video.mp4
          
          if [[ "$FILE_TYPE" != *"ISO Media"* && "$FILE_TYPE" != *"MP4"* ]]; then
             echo "CHYBA: Súbor nie je platné video."
             exit 1
          fi

      - name: Streamovanie na YouTube
        env:
          YOUTUBE_KEY: ${{ secrets.YOUTUBE_STREAM_KEY }}
        run: |
          if [ -z "$YOUTUBE_KEY" ]; then
            echo "CHYBA: YOUTUBE_STREAM_KEY nie je nastavený v GitHub Secrets!"
            exit 1
          fi

          echo "Spúšťam stream na YouTube..."
          
          # Pridané logovanie a kontrola pred štartom
          ffmpeg -re -stream_loop -1 -i video.mp4 \
          -c:v libx264 -preset veryfast -b:v 3000k -maxrate 3000k -bufsize 6000k \
          -pix_fmt yuv420p -g 50 -c:a aac -b:a 128k -ar 44100 \
          -f flv "rtmp://a.rtmp.youtube.com/live2/$YOUTUBE_KEY"
